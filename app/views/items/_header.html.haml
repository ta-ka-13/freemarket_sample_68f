.header
  .header__main
    .contents-top
      = link_to "#", class: "furima_link" do
        = image_tag "material/logo/logo.png", alt: "FURIMA", class: "furima_image"
      = form_with url: "#", method: :get, local: true do |f|
        .search-box
          = f.text_field :search, class: "search-box__text", placeholder: "キーワードから探す"
          = f.button class: "submit-btn" do
            = image_tag "material/icon/icon-search 1.png", alt: "検索", class: "search-icon"
    .contents-bottom
      .contents-bottom__right
        .right-list
          .right-list__category
            カテゴリー
          .right-list__bland
            ブランド
      .contents-bottom__left
        .left-list
          = link_to "#", class: "left-list__login" do
            ログイン
          = link_to "#", class: "left-list__new" do
            新規会員登録
          -# - if user_signed_in?
          -#   = link_to "#", class: "left-list__logout" do
          -#     ログアウト
          -#   = link_to "#", class: "left-list__mypage" do
          -#     マイページ
          -# - else
          -#   = link_to "#", class: "left-list__login" do
          -#     ログイン
          -#   = link_to "#", class: "left-list__new" do
          -#     新規会員登録